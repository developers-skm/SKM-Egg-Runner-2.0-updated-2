/**
 * InAppToast — removed.
 * All notifications are delivered via Android push (FCM) only.
 * This file is kept as a no-op so any stale import doesn't break.
 */
export default function InAppToast() { return null; }
